import { ViewStyle } from 'react-native';

const container: ViewStyle = {
    backgroundColor: '#F1F7ED',
    borderTopColor: '#001011',
    borderTopWidth: 3,
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    width: '100%',
}

export default {
    container,
}